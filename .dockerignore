# Default to ignoring the entire monorepo, then allow only what the web
# Dockerfile copies into the image.
**

!.dockerignore
!package.json
!yarn.lock
!tsconfig.json
!tsconfig.base.json
!apps/
!apps/web/
!apps/web/**
!db/
!db/migrations/
!db/migrations/**
!design-system/
!design-system/**
