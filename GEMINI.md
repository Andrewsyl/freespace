# FreeSpace — Gemini instructions

The canonical project instructions live in AGENTS.md (single source of truth shared across AI
tools). Import and follow everything in it:

@./AGENTS.md

If your runtime does not resolve the import above, read the file `AGENTS.md` at the repository
root before doing anything else — it contains the stack overview, money/booking invariants,
coding conventions, mobile UI rules, commands, and known landmines for this codebase.

Also useful: `docs/ENGINEERING_HANDBOOK.md` §20–21 (fast context) and
`docs/AI_PROMPT_LIBRARY.md` (reusable task prompts). If you update project conventions, edit
`AGENTS.md` (not this file) so all tools stay in sync.
