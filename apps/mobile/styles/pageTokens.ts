/**
 * The listing/booking-review palette, read from the app theme.
 *
 * These names stay because the kit and both screens read them; the values now
 * live in `styles/theme.ts` so there is one source of truth rather than a
 * parallel set.
 */
import { colors } from "../styles/theme";

export const INK = colors.pageInk;
export const MUTED = colors.pageMuted;
export const RULE = colors.pageRule;
export const PILL = colors.pagePill;
export const ACCENT_SOFT = colors.pageAccentSoft;
export const GREEN_DARK = colors.pageAccentDark;
export const GREEN = colors.primary;
export const WHITE = colors.appBg;
export const MAP_GROUND = colors.pageMapGround;
