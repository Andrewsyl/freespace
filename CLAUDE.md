# FreeSpace — Claude Code instructions

The canonical project instructions live in AGENTS.md (single source of truth shared across AI
tools). Follow everything in it:

@AGENTS.md

Claude-specific notes:

- Deep reference: `docs/ENGINEERING_HANDBOOK.md` (read §20–21 before non-trivial work).
  Reusable task prompts: `docs/AI_PROMPT_LIBRARY.md`.
- Prefer `npm run test:api`, `typecheck:mobile`, and `prepush:check` as verification commands.
- If you update project conventions, edit `AGENTS.md` (not this file) so all tools stay in sync.
